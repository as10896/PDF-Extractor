## Install:
`$ pip install pipenv`
`$ pipenv install`
then type `$ pipenv shell` to activate this project's virtualenv

## Start server:
`$ FLASK_APP=server.py flask run --port 9998`

## Execute client:
`$ python -i client.py`
